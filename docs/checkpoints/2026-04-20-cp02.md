---
status: done
---

# CP02

Previous: CP01 closed at 237 lines covering workflow bootstrap (S1),
v0.8.1/v0.8.2 null-field schema hotfixes (S2), and v0.8.3 panel-type
gate + diagnostic probe + CLI (S3). See
`docs/checkpoints/2026-04-17-cp01.md`.

## S1: v0.8.4 "unavailable" flapping fix + auto-arm post-check (2026-04-20)

**Shipped:** v0.8.4 (PyPI + GitHub + HACS), plus three new automation
patterns deployed to the user's live HA config and documented in
`docs/automations.md`.

### Root cause investigation — two failure modes

Same symptom (`alarm_control_panel.verisure_alarm` → `unavailable`),
two distinct causes:

- **Mode A — brief flapping** (~8×/day): single-tick HTTP blips
  (TCP reset, 5xx) bounced the coordinator to `unavailable` for ~5s,
  recovered next poll. Annoying but not dangerous.
- **Mode B — sticky 6h outage** (2026-04-20 10:11→16:04 UTC):
  `login()` caught a GraphQL upstream Node.js error
  (`Cannot read properties of undefined (reading 'it')`) and re-raised
  it as `AuthenticationError`. HA translated that to
  `ConfigEntryAuthFailed` — Repair card, zero auto-retry. Integration
  dormant until manual HA restart. **Security-critical:** arm
  automations silently dropped for 6 hours.

### Fix — two layers

1. **HTTP-layer retry**: `_execute()` retries `APIConnectionError` and
   5xx `APIResponseError` up to 3 times inside a single coordinator
   tick. Exponential backoff 5s → 10s with ±20% jitter, cap implicit
   via tick budget. 4xx + WAF + session expiry + 2FA are **never**
   retried (each has its own recovery path). `random.random()` jitter
   not `secrets` — retry timing is not cryptographic.
2. **Remove blanket auth classification**: `login()` no longer
   converts `APIResponseError → AuthenticationError`. `AuthenticationError`
   is now reserved for confirmed credential bugs (null auth token,
   malformed JWT). Generic upstream errors propagate as
   `APIResponseError` → `UpdateFailed` → coordinator's normal
   transient-failure path. True credential problems still reach the
   Repair card.

### Why no SMS risk

Confirmed: `login()` in normal operation doesn't trigger SMS — device
trust tokens are persistent, persist across restarts. SMS only on
explicit 2FA path (separate exception type). User explicitly rejected
"automatic SMS on auth retry" design.

### Design constraints preserved

- **Fail-secure**: errors surface, not swallowed. Three retries, then
  `UpdateFailed` → `unavailable` entity state → downstream automations
  can react.
- **No binary-sensor hiding**: user chose to keep `unavailable` state
  visible. Automations opt-in to it explicitly (see post-check below).
- **No PIN**: out of scope, HA is authenticated, Verisure API requires
  credentials.

### Tests

- `TestTransientRetry` (4 cases): 5xx retry-then-succeed, 5xx exhaust,
  4xx no-retry, connection-error retry.
- `no_sleep` fixture: patches `verisure_italy.client.asyncio.sleep` →
  no-op. Tests complete in ms, production code still sleeps.
- Updated existing tests to register 3 mocks per call (retry consumes
  multiples).
- Renamed `test_graphql_error_raises_auth_error` →
  `test_graphql_error_propagates_as_api_error` (semantics flipped).

### Auto-arm failure investigation

User reported: "today the auto arm automation didn't arm and didn't
notify." Ran `verisure-investigate` skill (3 parallel agents).

Evidence:
- HA log at 14:00:47 UTC: `WARNING: Referenced entities
  alarm_control_panel.verisure_alarm are missing or not currently
  available` — service call dropped because entity was `unavailable`.
- Automation `1765795470718` `last_triggered: 14:00:47Z` — fired but
  state never changed.
- Root cause: same Mode B outage. v0.8.4 prevents the entity-state
  cause going forward.

But the investigation revealed **three design gaps independent of the
backend bug**:

1. Primary arm automation had no post-check — silent failure.
2. Safety net condition `state: disarmed` fails when state is
   `unavailable` (correctly, we do NOT want to arm a ghost entity) —
   but no notification when this happens.
3. No alert when the entity has been `unavailable` for a prolonged
   period — the user doesn't know arm/disarm is dead until they try.

### Fixes (ha-config repo)

- `1765795470718` (primary arm): `wait_for_trigger` up to 60s for
  state → `armed_away`; `choose` branches critical notify on timeout,
  normal notify otherwise.
- `1765796183149` (safety net): same post-check pattern.
- **New**: `alarm_unavailable_alert` — state trigger `unavailable
  for: 00:05:00` → critical push to both phones. 5min sweet spot:
  transient glitches (now retry-absorbed) don't fire, real outages
  do. Labeled `alarm` via `ha-ws` entity_registry update.

All three patterns documented in `docs/automations.md` with WHY
comments, not just YAML.

### Release + deploy pattern

- Deployed client lib to HA container via
  `docker exec homeassistant sh -c 'cat > /usr/local/lib/...'`
  **before** publishing to PyPI — validated fix live before tagging.
- Renamed `verisure_italy-0.8.3.dist-info` → `0.8.4.dist-info` +
  patched METADATA to prevent pip from re-installing on next restart.
- Standard release via `verisure-release` skill after validation.
- HACS update entity lagged (showed 0.8.0) — **learning**: verify
  deployed version via SSH + `pip show` / `manifest.json`, never
  trust HACS update entity. Saved as
  `feedback_verify_deployed_version.md`.

### Stats

- Commits: `9de2231` (v0.8.4 client fix) + `c309e2d` (docs patterns).
- ha-config: `92376d0` (alarm automations) + `99d2a60` (HA UI drift).
- Tests: 4 new cases, full suite green, pyright strict clean, ruff clean.
- Findings: `docs/findings/unavailable-flapping.md` — full post-mortem.
- Issue #1: no new reporter response since v0.8.3 probe request
  (16:11 UTC). Still OPEN, waiting on CENT probe.

## S2: v0.8.5 Mode C — stale capabilities token loop (2026-04-20)

**Shipped:** v0.8.5 (PyPI + GitHub + HACS). Released ~2.5h after
v0.8.4 in response to a fresh in-production outage.

### The regression that wasn't

At 21:07 UTC (a few hours after v0.8.4 deployed), the alarm entity
went `unavailable` again and stayed stuck for 10+ minutes. The
`alarm_unavailable_alert` automation (deployed in S1) fired correctly
after 5 minutes — first real-world validation of the new automation.

Root cause was a **new failure mode independent of Mode A/B**:

- Verisure API uses two JWTs per authenticated call: `_auth_token`
  (from `login()`) and `_capabilities[installation]` (from
  `get_services()`). Both load-bearing; server rejects on either.
- `_ensure_auth` refreshes each independently based on the local JWT
  `exp` claim. But the server can invalidate a session **while the
  local `exp` is still fresh** (purge, restart, multi-device login).
- Coordinator had `except SessionExpiredError → login() + retry`. It
  refreshed auth but `_ensure_auth` saw cached capabilities
  "not expiring" → skipped `get_services()` → same stale capabilities
  re-sent → same 403 → infinite 5s tick loop until HA restart.

The second `SessionExpiredError` (from the retry) escaped every
handler, logged as "Unexpected error fetching verisure_italy data",
no `ConfigEntryAuthFailed` fired. v0.8.4 retry layer didn't help —
wrong layer, GraphQL post-HTTP.

### Fix

In `_execute`, wrap `_check_graphql_errors` and on
`SessionExpiredError` nuke both cached tokens before re-raising:

```python
try:
    self._check_graphql_errors(response_text, operation)
except SessionExpiredError:
    self._auth_token = None
    if installation is not None:
        self._capabilities.pop(installation.number, None)
        self._capabilities_exp.pop(installation.number, None)
    raise
```

Recovery flow: first 403 nukes caches → coordinator's existing
`login()` + retry → retry's `_ensure_auth` sees capabilities missing
→ calls `get_services()` → fresh capabilities → Status succeeds.
Next Mode C event recovers in 1-2 ticks (5-10s unavailable) without
restart.

### KISS design decision

User pushed back on "nuke both auth AND capabilities" — "keep valid
tokens, refresh invalid". Resolved: auth and capabilities are tied
to the same server session (capabilities is minted via an
authenticated call, so "capabilities dead + auth alive" is not a
realistic server state). A 403 doesn't tell us which one the server
rejected. Surgical invalidation needs cascading 403 handling across
operations, doesn't save anything measurable. **Nuke both, one extra
login() per rare recovery event.**

### Tests

- `TestSessionExpiredInvalidation` (2 cases):
  - installation-scoped op: both auth + capabilities[num] cleared.
  - installation-less op (list_installations): auth cleared, other
    installations' capabilities untouched.
- Full suite: 239 passed / 7 skipped. pyright strict clean.

### Release pattern

Deploy-first validated again: deployed `client.py` to container +
bumped `verisure_italy-0.8.4.dist-info` → `0.8.5.dist-info` + patched
METADATA Version. Reloaded integration via HA API, entity recovered
to `disarmed` cleanly. Arm/disarm tested live before `verisure-release
0.8.5`. One commit: `1b21b41 fix: invalidate cached tokens on
SessionExpiredError (v0.8.5)`.

### Stats

- Commits: `1b21b41` (code + version + docs).
- Tests: +2 cases, 239 total green.
- Findings: `unavailable-flapping.md` — Mode C section added, fix
  rationale documented including the KISS decision.
- Symptoms: first real-world fire of the S1 `alarm_unavailable_alert`
  automation — notification landed at 21:12:47 UTC, exactly 5min
  after unavailable started. Threshold validated.
